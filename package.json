{
  "name": "@a1/ai",
  "version": "0.1.0",
  "private": true,
  "description": "A1 product family \u2014 shared, framework-agnostic AI provider core: OpenRouter live model menu, per-aspect model policy, local AI settings store, and the opt-in Open Notebook connector. Consumed by every A1 product via dependency injection (inject safeFetch + a data-dir resolver).",
  "type": "commonjs",
  "main": "index.js",
  "exports": {
    ".": "./index.js"
  },
  "files": [
    "index.js",
    "src/"
  ],
  "scripts": {
    "test": "node --test",
    "karpathy:run:di-contract": "node evals/di-contract-frozen/check.js",
    "karpathy:list": "node scripts/karpathy-lane.mjs --list",
    "karpathy:program": "node scripts/karpathy-lane.mjs --program",
    "karpathy:run": "node scripts/karpathy-lane.mjs --run"
  },
  "engines": {
    "node": ">=22.5"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "acorn": "^8.12.0"
  }
}
